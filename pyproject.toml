[project]
name = "stavid"
version = "0.01"
description = "Created a bot to help us manage our apartment"
readme = "readme.md"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff]
lint.extend-select = ["I"]